{
  "name": "si-overseas",
  "version": "1.0.0",
  "description": "SIBIONICS fault detect demo rebuilt with Vue 3 and TypeScript.",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --configLoader native",
    "build": "vue-tsc -b && vite build --configLoader native",
    "build:netlify": "node scripts/verify-no-legacy-views.mjs && npm run build",
    "preview": "vite preview --host 0.0.0.0 --configLoader native",
    "typecheck": "vue-tsc -b",
    "test": "vitest run --configLoader runner"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "vue": "^3.5.33",
    "vue-router": "^5.0.6"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@vitejs/plugin-vue": "^6.0.6",
    "@vue/test-utils": "^2.4.8",
    "@vue/tsconfig": "^0.9.1",
    "jsdom": "^29.1.0",
    "playwright": "^1.60.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.10",
    "vitest": "^4.1.5",
    "vue-tsc": "^3.2.7"
  }
}
